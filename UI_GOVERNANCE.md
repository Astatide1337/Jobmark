# Jobmark UI governance

The frontend rules live in [`docs/frontend-standards.md`](docs/frontend-standards.md).

This file remains as a stable link for existing contributors and tooling. It is not a
second source of truth. The old `uiV2` flag and `UIProvider` migration path were
removed; new UI work should follow the current shell, motion, token, and accessibility
rules in the canonical document.
