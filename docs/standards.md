# Code standards

The canonical frontend and code-quality rules are maintained in
[`frontend-standards.md`](frontend-standards.md).

This file is retained as a compatibility link for older references. In particular,
the repository uses `lib/themes.ts`, not `lib/theme.ts`, and Tailwind v4 is checked
by the repository's frontend invariant script rather than the disabled Tailwind ESLint
plugin.
